interface Window {
  dataLayer?: object[];
  ApplePaySession: any;
}

interface GTMProducts {
  id: string;
  name: string;
  category?: string;
  brand?: string;
  price?: string;
  quantity: number;
}
